<Window x:Class="Todolist.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Todolist"
        mc:Ignorable="d"
        Title="MainWindow" Height="800" Width="800" ResizeMode="NoResize">
    <Grid>
        <TextBox x:Name="TaskBox" HorizontalAlignment="Center" Margin="0,717,0,0" TextWrapping="Wrap" Text="Enter text here..." VerticalAlignment="Top" Width="312" FontSize="20">
            <TextBox.Effect>
                <DropShadowEffect/>
            </TextBox.Effect>
        </TextBox>
        <Button x:Name="AddTask" Content="Add Task" HorizontalAlignment="Left" Margin="578,717,0,0" VerticalAlignment="Top" Click="AddTask_Click" Height="33" Width="90"/>
        <ListBox x:Name="ToDoList" Margin="24,25,22,97" FontSize="20">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding TaskName}" Width="200" VerticalAlignment="Center"/>
                        <Button Content="Delete" Click="DeleteTask_Click" Tag="{Binding}"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

    </Grid>
</Window>
